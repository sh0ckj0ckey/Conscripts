<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Conscripts.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Conscripts"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Conscripts"
    Closed="Window_Closed">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Window.Content>
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Frame x:Name="MainFrame" Margin="0" Padding="0,64,0,0" Loaded="MainFrame_Loaded" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

            <Grid x:Name="TitleBar" Height="64" Background="Transparent" BorderBrush="{ThemeResource ControlElevationBorderBrush}" BorderThickness="0,0,0,1" HorizontalAlignment="Stretch" VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Image Grid.Column="0" Margin="20,0,0,0" Height="32" Width="32" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Image.Source>
                        <BitmapImage UriSource="/Assets/Conscripts.png" DecodePixelType="Logical" DecodePixelHeight="64"/>
                    </Image.Source>
                </Image>
                <TextBlock Grid.Column="1" Margin="12,0,0,0" Text="{x:Bind TitleBarViewModel.AnimatedTitleText, Mode=OneWay}" FontFamily="{ThemeResource CascadiaCodeFont}" FontWeight="Bold" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Grid>
    </Window.Content>
</Window>
